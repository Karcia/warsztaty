.newest-reviews.row
  .col-md-1
  .col-md-10
    %h4 Newest reviews:
    %table.table.table-striped
      %thead
        %tr
          %th #
          %th Rating
          %th Content
          %th Author
      %tbody
        - product.reviews.order("created_at DESC").each do |r|
          %tr
            %td= r.id
            %td= r.rating
            %td= r.content
            %td= link_to r.user.full_name, user_path(r.user), class: "btn btn-link" if r.user
  .col-md-1
